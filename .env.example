# ============================================
# Environment variables for Motocare (Vite)
# ============================================
# Copy this file to `.env` (local) or set the variables in your hosting provider (Vercel/Netlify).
# NEVER commit .env files with real credentials to git!
# ============================================

# Supabase project (REQUIRED)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://YOUR-PROJECT-NAME.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_PUBLIC_KEY

# Service role key - ONLY for server-side scripts, NEVER expose in frontend!
# SUPABASE_SERVICE_ROLE=YOUR_SERVICE_ROLE_KEY

# Optional app settings
VITE_APP_NAME=Motocare
VITE_LOG_LEVEL=info

# ============================================
# SECURITY NOTES:
# - The ANON_KEY is designed to be public (used with RLS)
# - The SERVICE_ROLE_KEY bypasses RLS - NEVER expose it
# - For AI/API features, use backend proxy instead of exposing keys
# ============================================
